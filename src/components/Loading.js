import React from "react";

export default function Loading() {
  return (
    <h2
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        flex: "1"
      }}
    >
      Loading...
    </h2>
  );
}
